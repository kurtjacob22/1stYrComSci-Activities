#include <iostream>
using namespace std;

int main() {

    int x, y;
    
    cout << "Enter first number:";
    cin >> x;

    y = x;

    cout << "Enter second number:";
    cin >> x;

    cout << "The value of x = " << x;
    cout << "\nThe value of y = " << y;

    // int x, y;
    
    // cout << "Enter first number:";
    // cin >> x;

    // cout << "Enter second number:";
    // cin >> y;

    // x = x+y;
    // y = x-y;
    // x = x-y;

    // cout << "The value of x = " << x;
    // cout << "\nThe value of y = " << y;

    // int w = 3, x = 25, y = 10, z = 2;
    // float a = 2, b = 43, c = 11, d = 12;


    // cout << (w * x / y * z) % c * (b % c);

    //cout << 11.3 + 3.5 * 24.1 - 5.6 / 4.1;

    return 0;
    
}


